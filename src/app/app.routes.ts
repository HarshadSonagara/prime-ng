import { Routes } from '@angular/router';
import { DynamicJsonFormComponent } from './components/dynamic-json-form/dynamic-json-form.component';

export const routes: Routes = [
    { path: '', component: DynamicJsonFormComponent }
];
